# chrony client config for ZED fleet — sync to PC (managed by setup)
server 192.168.0.50 iburst minpoll 3 maxpoll 6
makestep 1.0 3
driftfile /var/lib/chrony/chrony.drift
rtcsync
logdir /var/log/chrony
