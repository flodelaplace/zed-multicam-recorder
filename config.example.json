{
  "_comment": "Fleet config for zed-multicam-recorder. Copy to config.json and edit for your setup.",

  "port": 9999,
  "remote_dir": "/tmp/recordings",

  "default_resolution": "HD720",
  "default_fps": 30,

  "openblas_armv8": true,

  "default_ssh_user": "zed",

  "calibration": {
    "repo": "/home/<user>/extrinsic-camera-calibration/lab-camera-dynamic-calibrator",
    "conda_env": "human_calib",
    "script": "scripts/calibrate.sh",
    "device": "cuda",
    "mode": "balanced",
    "pose_engine": "metrabs",
    "frame_skip": 5
  },

  "hosts": [
    { "ip": "192.168.0.2", "user": "zed1", "label": "cam_left",       "rotate": 0   },
    { "ip": "192.168.0.3", "user": "zed",  "label": "cam_back_left",  "rotate": 0   },
    { "ip": "192.168.0.4", "user": "zed",  "label": "cam_back_right", "rotate": 90  },
    { "ip": "192.168.0.6", "user": "zed",  "label": "cam_right",      "rotate": 270 }
  ],

  "_explanations": {
    "port": "TCP port that each recorder listens on. Must match zed_recorder.py --port.",
    "remote_dir": "Path on each Jetson where SVOs and sidecar CSVs land. Must be writable by the SSH user. /tmp is fine for tests but cleared on reboot — use /home/<user>/recordings or an SSD mount for production.",
    "default_resolution": "ZED2 supports HD2K (15 fps max), HD1080 (30 fps), HD720 (60 fps), VGA (100 fps). HD720@30 is the validated setting on a 12-cam fleet (HD1080 has a periodic-drop SDK bug, HD720@60 drops ~13%).",
    "calibration": "Extrinsic calibration launched from the web dashboard on synced MP4s (essai_<N>/raw_mp4/). Points to an external repo run through 'conda run -n <conda_env>'. Optional 'conda_bin' (default ~/miniconda3/bin/conda).",
    "hosts.serial": "Optional. ZED2 serial number of the camera plugged into this host (used to read /usr/local/zed/settings/SN<serial>.conf intrinsics).",
    "default_fps": "Capped by the resolution choice above.",
    "openblas_armv8": "Set to true for Jetson Nano (Cortex-A57 cannot run modern numpy BLAS dispatch). Set to false on Xavier NX or Orin where it is not needed.",
    "default_ssh_user": "Used when --hosts overrides config, or when a host entry omits 'user'.",
    "hosts.user": "Per-Jetson SSH login. Often differs across machines if they were flashed by different people.",
    "hosts.label": "Friendly identifier used in output tables and pull subdirectory names. Must be unique across hosts.",
    "hosts.rotate": "Optional. Degrees of clockwise rotation applied to the cam's frames when sync_align.py writes the aligned MP4. Use 0 for landscape, 90/180/270 if the camera is physically mounted on its side. Has no effect on the live recorder — it's a post-processing knob applied at alignment time."
  }
}
